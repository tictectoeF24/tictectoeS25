{
  "scripts": {
  "frontend": "cd tictectoe-frontend && npm install && npx expo start",
  "dev": "cd tictectoe-backend && npm install && nodemon app.js",
  "start": "cd tictectoe-backend && npm install && node app.js",
  "test-server": "cd tictectoe-backend && node test-server.js",
  "dev:all": "concurrently \"npm run dev\" \"npm run frontend\""
},
  "dependencies": {
    "@expo/vector-icons": "^14.0.4",
    "@supabase/supabase-js": "^2.52.1",
    "express": "^4.21.2",
    "react-native-url-polyfill": "^2.0.0",
    "@google/genai": "^1.9.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}